# SimCore v0.79 UI 개조 작업 — 원작자 전달용 인수인계

> 이 문서는 실제 PR이 아니라 원작자가 비공식 UI 작업의 범위와 병합 가능성을 검토할 수 있도록 작성한 PR 형식의 인수인계입니다. 커밋·푸시·PR 생성은 하지 않았습니다.

## 제안 PR 제목

`ui: refine authoring hierarchy and status editor on v0.79 baseline`

## 요약

공식 SimCore v0.79.0의 엔진·스키마·게임 패널·모델 호출 어댑터를 보존하면서 관리 패널과 `editor` 모듈의 정보 위계, 입력 폭, 오류 가독성, 접기와 이동 흐름을 정리한 비공식 UI 작업입니다.

작업본은 사용자 피드백을 여러 차례 반영한 실험판입니다. 한 번에 전부 병합하기보다 표시 중심 변경, 공통 시각 체계, 편집기 보조 동작을 나누어 검토하는 편이 안전합니다.

## 기준 파일

| 구분 | 파일 | SHA-256 |
| --- | --- | --- |
| 공식 기준선 | `reference/original/simcore.plugin.v0.79.0.reference.js` | `4B42153BB2D86B7D7FFCC6E6B094E29E42673C202572463C00200EBD7A421CD5` |
| UI 작업본 | `simcore.plugin.js` | `B3A32A6AAFFE5A1A0584C64300A3E6E3D949998448CE52CAD0A17198E3FC4B5A` |
| v0.79 병합 전 UI 스냅샷 | `reference/migration/simcore.plugin.v0.61-ui-before-v0.79.reference.js` | 비교·이식 참고용 |

공식 기준선 파일은 수정하지 않았습니다.

현재 작업본은 호환성을 위해 내부 ID `simcore`와 버전 `0.79.0`을 유지합니다. 표시 이름 `SimCore (시뮬 엔진) v0.79 지적 접기`는 비공식 작업본 구분용이므로 공식 병합 시 원작자의 명명 규칙으로 되돌리면 됩니다.

## 변경 원칙

1. 공식 기능과 스키마 계약을 UI보다 우선했습니다.
2. 내부 ID, 공식 DOM ID, 이벤트 핸들러와 공개 API를 유지했습니다.
3. `editor` 이외 보호 코어와 `#sc-game` 런타임은 기준선과 분리해 감사했습니다.
4. Provider Manager나 SimCore 전용 provider 기능을 추가하지 않았습니다.
5. 보조 모델·생성 모델 호출 어댑터를 변경하지 않았습니다.
6. 작은 반경, 얇은 선, 그래파이트 표면과 제한적인 파란색 강조를 사용했습니다.
7. 정적 검사와 실제 Risu 검증을 명확히 구분했습니다.

## 원본 대비 변경 지도

### 1. 공통 시각 체계와 반응형

- 제목·본문·보조 문구·입력·placeholder·상태색의 대비를 공통 토큰으로 정리했습니다.
- 큰 둥근 카드 중첩을 줄이고 제목, 구분선, 간격과 정렬로 정보 위계를 만들었습니다.
- 짧은 ID·숫자·선택 필드와 긴 프롬프트·JSON·CSS·HTML 입력을 같은 폭으로 취급하지 않습니다.
- 1040px·840px·760px·600px 부근에서 필요한 작업 영역만 1열로 전환합니다.
- 키보드 포커스와 선택·비활성 상태가 색상만으로 구분되지 않도록 문구와 구조를 함께 사용합니다.

### 2. 관리 패널

#### 현황

- 새 시작, 다음 행동, 상태 변수, 엔진 요약, 클릭 조작과 보조 모델 연결을 역할별로 분리했습니다.
- 상태 변수 수동 보정의 짧은 값을 압축하고 가로 스크롤 가능성을 낮췄습니다.
- 잠긴 액션은 비활성 이유와 안내가 가까이 보이도록 했습니다.
- 세션 시계를 작업본 감지 전 첫 화면에서도 표시합니다.

#### 도움말

- 공식 개념을 한 턴 진행 순서와 변수·파생·규칙·액션·AI 설정 순으로 재배치했습니다.
- `int / float / text / bool / enum / list`를 설정·갱신·예시가 구분되는 정적 매뉴얼로 만들었습니다.
- 임의 이동이나 실행 동작은 도움말에 추가하지 않았습니다.

#### AI 어시스턴트

- 창작·결과·진단의 책임을 분리하고 처음 설치 01~04 안내와 실패 정보를 정리했습니다.
- 결과 생성의 `스킨만 / 배치까지`를 독립 선택으로 유지하고 디자인 요청 입력을 확대했습니다.
- Hallmark 보정은 기존 생성 프롬프트를 교체하지 않고 사용자가 켰을 때만 추가 지침을 붙입니다.
- 생성 결과에서 변수·이벤트·액션·판정·지시문 개수와 이벤트의 발동·조건·효과·통지를 구분합니다.
- 진단 결과는 중요도별 목록과 부분 AI 수정 요청 흐름으로 정리했습니다.

### 3. JSON 관리자

- 전체 작업본과 부분 수정 요청을 2열 작업대로 구성하고 모바일에서는 1열로 전환합니다.
- 검사, 불러오기 전 구성 요약, 적용, 되돌리기 상태를 구분했습니다.
- 규격 복사 결과가 긴 화면을 계속 차지하지 않도록 다시 닫는 동작을 추가했습니다.
- 전체 스키마 교체와 부분 패치의 공식 함수는 그대로 사용합니다.

### 4. 에셋 관리자

- 연결 설정, 도구, 비용 요약, 검증 상태와 팩 목록의 위계를 정리했습니다.
- 팩 설정을 예상 값 길이에 맞춘 두 줄로 압축했습니다.
- 팩 접기, 오류 개수, 슬롯 설정, 폴백 팝오버와 팩 내부 드래그 이동을 추가·정리했습니다.
- 에셋 해석·검증·모델 연결 코어는 변경하지 않았습니다.

### 5. 변수 편집기

- 기본 변수의 공통 정보 순서를 다음 세 영역으로 통일했습니다.
  1. 변수 ID와 값 형식
  2. 시작값·최소·최대 등 형식별 입력
  3. 상태창 미리보기와 AI 설명
- 정수·실수·텍스트·ON/OFF·선택지·목록마다 필요한 입력만 표시합니다.
- 텍스트·선택지·목록은 여러 줄 편집을 사용하며 선택지 내부 쉼표를 손상시키지 않습니다.
- 내부 스키마 경로나 `enum`, `null` 같은 원시 용어를 쉬운 오류 문장으로 변환합니다.
- 숫자 입력 오류는 네 칸 아래 전체 폭 경고줄로 표시해 열이 무너지지 않게 했습니다.
- 참조 중인 ID 변경 경고, 삭제 되돌리기, 카드 접기, 오류만 펼치기를 추가했습니다.
- 기본·파생 변수를 함께 검사하는 자동 ID와 드래그 순서 이동을 지원합니다.

### 6. 명령 편집기

- 연결 변수를 먼저 고르고 해당 형식에 맞는 명령과 사용 예시를 만드는 흐름으로 정리했습니다.
- 정수·실수·텍스트·ON/OFF·선택지·목록별 예시와 문법 설명을 가까이 표시합니다.
- 중복·임시 ID와 목록 합산 판정 때문에 편집 화면이 비지 않도록 editor 측 방어를 추가했습니다.
- 빈 상태, 문제만 펼치기, 새 카드 위치 안내와 AI 작업대 폭 정렬을 적용했습니다.

### 7. 상태창 편집기

#### 핵심 설정

- 상태창 제목, 구성 방식, 테마를 첫 행에 배치했습니다.
- 변화 로그와 여러 그룹 배치를 둘째 행의 같은 좌측 작업 폭으로 정렬했습니다.
- 메시지 표시와 상태창 접기 허용을 오른쪽 별도 열로 유지했습니다.
- 기본 테마의 개발자용 영문명을 사용자용 한글 이름으로 표시하고 작은 결과 미리보기를 추가했습니다.
- 미리보기는 메시지 표시 설정 아래에 두어 선택과 결과를 한 영역에서 확인합니다.

#### 자동 구성과 커스텀 HTML

- 자동 구성·커스텀 HTML·조건부 템플릿의 기존 데이터 구조는 유지했습니다.
- 커스텀 HTML 편집 설명을 기본 템플릿, 배치 뼈대, 조건부 템플릿의 3단계로 나눴습니다.
- 탭·접기/펼치기·버튼 팝업 뼈대의 목적과 삽입 위치를 가까이 설명합니다.

#### 그룹과 개별 항목

- 그룹 설정은 이름, 초기 상태, 표시 조건과 합치기를 예상 길이에 맞춘 열로 배치합니다.
- 그룹과 항목 모두 드래그 손잡이, 고스트, 삽입 위치와 1.5초 이동 피드백을 지원합니다.
- 개별 항목의 기존 5열 표 구조를 2단 정보 구조로 바꿨습니다.
  - 첫 줄: 항목 번호·표시 값 / 형식·범위 / 실제값 미리보기 / 조작
  - 둘째 줄: 게이지 사용 / 최대값
- 게이지가 꺼져 있으면 최대값 필드를 숨깁니다.
- 표시 조건과 색상은 `조건·색상` 아래로 접습니다.
- 기존 설정이나 검증 오류가 있으면 고급 영역을 자동으로 펼칩니다.
- 초기 상태와 lookup을 이용한 편집기 전용 미리보기를 표시합니다.

## 편집기 UI에 추가된 상태·동작

다음은 단순 CSS 재배치가 아니므로 별도 검토가 필요합니다.

- 첫 설치 안내 닫기 상태
- 복사 결과 다시 닫기
- 변수 삭제 한 단계 되돌리기
- 변수·에셋·상태 그룹·상태 항목 드래그와 이동 피드백
- 변수 형식별 여러 줄 파서와 중복 없는 자동 ID
- 명령 편집기의 문제만 펼치기와 빈 화면 방어
- 진단 결과의 부분 수정 요청 작성 흐름
- 상태 항목의 조건·색상 펼침 상태와 초기값 미리보기

이 상태들은 엔진 실행 상태가 아니라 편집기 표시·조작 상태입니다.

## 표시와 배치 중심 변경

- 현황, 도움말, 세이브 화면
- AI 결과 도감과 진단 목록
- JSON 작업대의 정보 위계
- 에셋 팩 편집 화면
- 변수·명령·상태창의 필드 폭과 정렬
- 상태창 테마명·미리보기와 커스텀 HTML 안내

위 영역은 기존 공식 데이터와 실행 함수를 계속 사용합니다.

## 보호 범위와 정적 감사 결과

2026-08-09 마지막 검사에서 다음 계약이 공식 원본과 일치했습니다.

- 모듈 15개
- 메인 내비게이션 8개
- 세부 편집 탭 11개
- DOM ID 38개
- 핸들러 16개
- 핵심 함수 15개
- 공개 API 5개
- editor를 제외한 보호 모듈 14개
- 편성표·달력 게임 패널 런타임
- 보조·생성 모델 호출 어댑터

실행한 검사:

```powershell
node --check simcore.plugin.js
node tools/audit-official-ui-baseline.mjs
node tools/audit-protected-core.mjs
node tools/audit-p4-readability.mjs
node tools/audit-p5-outer-ui.mjs
node tools/audit-p5-1-aux-connection.mjs
```

6개 모두 통과했습니다.

## 실기 검증 상태

이번 세션에서는 사용자 요청에 따라 Risu를 실행하지 않았습니다. 따라서 정적 감사 결과를 실제 Risu 회귀 검증 완료로 간주하면 안 됩니다.

특히 최신 상태창 항목 2단 구조는 코드와 UI 계약 감사만 확인했고 실제 렌더 화면은 아직 보지 않았습니다.

## 아직 완료하지 않은 범위

다음 세부 탭은 공식 기능 보존과 공통 스타일 적용이 우선이었고 화면별 사용자 피드백 마무리가 부족합니다.

1. 편성표
2. 달력
3. 규칙·이벤트
4. 액션
5. 판정
6. 시간
7. 새 시작
8. AI 설정

남은 검증:

- 상태창 항목의 긴 변수명·오류·게이지 ID 참조·조건·색상 실기
- 기존 v0.79 스키마 불러오기와 저장 유지
- 변수·에셋·상태 그룹·항목 드래그
- JSON 전체 교체·부분 패치와 되돌리기
- 세이브 내보내기·가져오기
- 편성표·달력 편집기와 실제 게임 패널
- 360px·600px·840px와 데스크톱 폭

## 원작자 검토 제안

한 번에 전체를 병합하기보다 다음 순서로 분리하는 편이 안전합니다.

1. 공통 색상·타이포그래피·포커스·반응형 토큰
2. 현황·도움말·세이브 같은 표시 중심 UI
3. AI 결과 도감과 진단 목록
4. JSON·에셋 작업대의 표시 재배치
5. 변수·명령·상태창의 필드 폭과 오류 문구
6. 복사 결과 닫기와 접기 상태
7. 드래그·삭제 되돌리기·부분 수정 요청·상태 미리보기 같은 editor 전용 동작

6~7단계는 실제 Risu 회귀 검증 뒤 별도 커밋으로 분리하는 것을 권합니다.

## 번들 구성

- `simcore.plugin.js`: 현재 UI 작업본이자 정적 검사 실행 기준
- `reference/simcore.plugin.v0.79.0.reference.js`: 수정하지 않은 공식 기준선
- `reference/simcore.plugin.v0.61-ui-before-v0.79.reference.js`: 이전 UI 이식 참고본
- `source/CHANGES_FROM_OFFICIAL_v0.79.patch`: 공식 원본 대비 줄 단위 패치
- `ARCA_POST_SIMCORE_UI_MOD_v0.79_2026-08-09.html`: 일반 게시용 설명
- `05_NEXT_SESSION_HANDOFF_2026-08-09.md`: 다음 작업용 상세 인수인계
- `UPSTREAM_AUTHOR_HANDOFF_SIMCORE_UI_MOD_v0.79_2026-08-09.md`: 이 문서
- `00_DESIGN_RULES.md`: UI 판단 기준
- `04_SESSION_HANDOFF_2026-08-09.md`: 직전 작업 지침
- `tools/`: 필수 정적 감사 도구
- `RUN_STATIC_CHECKS.cmd`: Windows용 일괄 정적 검사
- `evidence/user-feedback/`: 사용자 피드백 설명과 스크린샷 57장
- `MANIFEST_SHA256.txt`: 번들 파일 해시

## 권리와 명칭

- 이 작업은 공식 SimCore를 기반으로 한 비공식 UI 실험입니다.
- 원작자의 공식 배포와 라이선스가 우선합니다.
- Hallmark 관련 자료는 번들에 재포장하지 않으며, 현재 작업 폴더의 별도 참고 자료와 해당 라이선스를 따릅니다.
- 공식 병합 또는 배포 시 표시 이름과 `불법 개조` 같은 농담성 표현은 원작자의 판단에 따라 제거하면 됩니다.
