# 아틀리에 레슬레리아나 — 채집·연금·경영 시뮬 (설계)

원본: 아틀리에 레슬레리아나 RP 봇 (로어북 151항목 / 카드 디스크립션 / **루아 없음**).
얼헌과 달리 변환할 스크립트가 없다 — **로어북에서 심코어로 옮길 것만 옮기고, 남은 로어북과
디스크립션을 정리하면 끝**이다.

목표는 원본의 "서사로 굴러가는 아틀리에"를 **숫자로 굴러가는 공방 경영 시뮬**로 바꾸는 것.
원본 상태창(로어북 145 — 년도/날짜/요일/시간/위치/평판/콜/아이템/소재를 매 응답 본문에
찍으라는 지시 740토큰)이 심코어 상태창 하나로 통째 대체된다.

---

## 1. 가장 어려운 문제 — 시작이 시리즈마다 다르다

제작자가 아틀리에 전 시리즈 인물을 넣고 **퍼스트 메시지를 계열별로 쪼개 놨다.**
소피로 시작하든 로로나로 시작하든 라이자로 시작하든, 공방 이름·거점·스승·동행이 전부 달라진다.
그런데 채집·연금·경영 규칙은 계열과 무관하게 공용이다.

### 답: `setup.ai` (세션 0) — 첫 응답이 곧 시작 설정이다

`isSetupPending = setup.ai.enabled && !meta.setupDone && meta.turn === 0`
([core/engine.js](../core/engine.js#L126)) — **세션 0은 "첫 생성 응답" 딱 한 턴이다.**
퍼스트 메시지는 생성물이 아니라 turn 0 이전이므로, 유저가 계열을 고르고 첫 입력을 넣은
그 응답이 정확히 설정 턴이 된다.

- `setup.ai.instruction` → **메인 프롬프트에** 실린다 (engine.js:808). 기본 문구
  ("아직 시뮬레이션이 시작되지 않았다")는 쓰지 않는다. 첫 장면이 이미 제시된 봇이므로:
  > `[첫 장면] 지금 응답이 이 판의 시작이다. 앞의 도입부와 유저의 첫 입력을 근거로 공방의
  > 이름·자리·스승·동행을 장면 안에서 자연스럽게 확정하라. 목록으로 나열하지 마라.`
- 이어서 어댑터가 그 응답 본문을 읽고 **전용 보조 호출**로 초기값을 뽑는다
  (`buildSetupPrompt` — 절대값, 델타 아님).
- `setup.ai.vars`에 적힌 변수만 채워진다. 여기 없는 건 못 건드린다.

**채우는 것**: `atelier_name`(공방 이름) · `atelier_place`(공방이 있는 곳) · `mentor`(스승·후견인)
· `origin`(동행 계열 enum) · `location`(현 위치) · `allies`(동행 명단)

**그리고 이 여섯은 `updater.allow`에 넣지 않는다.** 한 번 굳으면 보조 AI가 영영 못 흔든다 —
"공방 이름이 턴마다 미묘하게 바뀐다"는 사고가 구조적으로 안 난다.
유저 수정 통로는 변수 `cmd` 하나: `atelier_name.cmd = '공방'` → `/공방 별빛 공방`.

### `origin` enum이 NPC 목록 3,792토큰을 쪼갠다 (P3 ✅ 실측)

로어북 144 `npc 리스트`는 **always-on 3,792토큰**이다 (전 시리즈 캐스트 맵).
`origin`으로 지시문 8벌로 갈랐고, **실측 절감은 초반 85% / 후반 최악 55%**다.

| 언제 | 실리는 것 | 토큰 |
|---|---|---|
| 초반 (renown 30, clues 0) | 란타르나 코어만 | **562t** |
| 후반 최악 (renown 700, clues 5, 아를란드) | 코어 + 월영회 + 극야 + 아를란드 11인 | **1,697t** |
| 원본 (always-on) | 전부, 언제나 | 3,792t |

- 적대 세력은 **이야기가 거기까지 갔을 때만**: 월영회 `renown >= 100 or clues >= 1` ·
  극야 `clues >= 2 or renown >= 300`. 초반엔 크리셀다가 프롬프트에 없다.
- **손으로 옮기지 않는다.** 생성기가 빌드 때 `lorebook_export.json`에서 `### ` 구간을 잘라
  굽는다 — 구간을 못 찾으면 빌드가 즉시 죽는다(조용한 빈 지시문 방지). 영/한/일 이름 병기를
  그대로 살려서, 모델이 그 이름을 쓰면 개별 인물 로어북 항목이 뜬다.
- **enum 값은 로어북 144가 스스로 묶어 둔 구간을 그대로 따른다** — 원본이 잘부르그·그람나트를
  한 구간, 비밀·추억·기타를 한 구간으로 묶어 놨다. 다르게 쪼개면 구울 때 어긋난다:

```
origin: 란타르나 | 잘부르그·그람나트 | 아를란드 | 황혼 | 신비 | 비밀·추억
```

세계는 언제나 란타르나다 (이방인은 란타르나에 흘러든 사람들). `origin`은 "어느 세계냐"가
아니라 **"이 판의 동행이 어느 계열이냐"**다 — 봇 디스크립션의 이방인 규약과 어긋나지 않는다.
남의 계열 인물도 **이름을 부르면 그대로 등장한다** (개별 항목이 키워드로 뜨고, 로어북 146
`퓨처플랜`이 전체 프로필을 끌어온다).

⚠ 이걸 켠 뒤에는 **로어북 144를 반드시 지운다.** 안 지우면 3,792토큰이 그대로 또 실린다.

---

## 2. 시스템 지도 — 6기능 → 심코어 모듈

| 기능 | 심코어 | 비고 |
|---|---|---|
| **채집** | `⛏ 채집` 액션 + `gather` 판정 + `materials` 목록 | 등급이 "몇 칸 얻었나"를 주고, 무엇을 얻었는지는 서사가 |
| **탐험·탐사** | `🔍 탐사` 액션 + `survey` 판정 + `areas`·`clues` | 새 채집지 개방 / 유적·마나 이상 단서 |
| **연금** | `🧪 조합` 액션 + `synth` 판정 + `items`·`recipes` | 난이도는 `synth_tier` enum, 보정은 평판+가마 |
| **경영** | `shop`(왕도 상점) + `quests` 목록 + `calendar` + `party` 설비 탭 | 콜·기한·설비 투자 |
| **전투** | `checks[].fight` (v1.6 전투 안무) + `⚔`·`🏃` | 게이지가 차야 결착 — 한 응답에 안 끝난다 |
| **의뢰** | `board`(카페 의뢰 게시판, 자율형) → `quests` 목록 | 2단계 |

**탐험과 탐사를 나누지 않는다.** 원본에서 둘의 경계가 없다 — 이동은 서사고, 굴릴 값이 있는
쪽(유적 조사·새 채집지 발견)만 `survey` 판정 하나로 받는다.

---

## 3. 변수

### 정체성 (setup.ai 전용 — allow 금지)
| id | type | |
|---|---|---|
| `atelier_name` | text | 공방 이름. `cmd: '공방'` |
| `atelier_place` | text | 공방이 있는 곳 |
| `mentor` | text | 스승·후견인 |
| `origin` | enum | 동행 계열 9종 |

### 연금술사
| id | type | |
|---|---|---|
| `renown` | int 1~1000 | **평판 = 세상의 신용** (로어북 149의 5단 구간 그대로) |
| `stamina` | int 0~100 | 체력. 채집·조합·이동·피격이 깎고 휴식·식사가 올린다 |
| `synth_cat` | enum 6 | 지금 조합하는 **분야** (폭탄/약품/중간재/도구/음식/비전). `cmd: '분야'` |
| `synth_tier` | enum 3 | **레시피 난이도** (기초/고급/비전) — 로어북 문단이 곧 등급 (§4.3) |
| `sk_bomb` `sk_med` `sk_mat` `sk_tool` `sk_food` `sk_arcane` | int 0~100 | **분야별 숙련도** — 조합할 때마다 그 분야만 오른다 |
| `last_quality` | enum 5 | 직전 조합물의 품질 (걸작/상품/보통/조잡/실패). **판정 산물 — allow 금지** |
| `location` | **enum 17** | 현 위치 = 지형·권역 (§4.2). 고유명사가 아니다 |
| `foe_tier` | int 1~5 | 교전 상대의 격. 보통은 `area_tier`와 같고 강적만 위로 |
| `bombs` | int 0~20 | 투척 아이템 재고 |
| `quest_pay` | int 0~15000 | 납품 정산 대기액 (§4.1) |
| `quest_n` `quest_lost` | int | 만료 감지 카운터 (§4.1) — **allow 금지** |
| `clues` | int 0~ | 잊혀진 연금술·백색 혜성 단서 |

`derived alch_tier` — renown → `견습 / 기초 / 중급 / 상급 / 명인` 문자열 사슬.
**int + 코드북이 아니라 문자열을 직접 반환하는 파생**으로 (숫자 대응표 린트 회피).

> **평판과 숙련은 다른 축이다.** 평판은 "사람들이 나를 믿는가"(로어북 149 그대로),
> 숙련은 "이 분야를 얼마나 손에 익혔나". 조합 판정은 숙련이 주고 평판이 거들며,
> 의뢰·가격·태도는 평판이 정한다.

### 분야 6종 — 로어북에서 그대로 온다
로어북 38~43이 이미 깔끔하게 갈라 놨다. 새로 나누지 않는다.

| 로어북 | `synth_cat` | 숙련 변수 |
|---|---|---|
| 38 공통 공격 아이템 | 폭탄 | `sk_bomb` |
| 39 공통 회복 아이템 | 약품 | `sk_med` |
| 40 공통 중간재와 소재 | 중간재 | `sk_mat` |
| 41 채집 도구와 탐험 장비 | 도구 | `sk_tool` |
| 42 음식과 생활 아이템 | 음식 | `sk_food` |
| 43 희귀와 고위 연금술 | 비전 | `sk_arcane` |

### 자원
| id | type | |
|---|---|---|
| `cole` | int 0~ | 소지금 |
| `materials` | list 24 | 보유 소재 |
| `items` | list 20 | 보유 아이템 (이름 있는 것 — 수량 관리는 `bombs`) |
| `recipes` | list 30 | 습득 레시피 |
| `tools` | list 8 | 채집 도구 (곡괭이·낫·낚싯대·채집망…) — `count(tools)`가 채집 보정 |
| `areas` | list 12 | 개방된 채집지 = **순수 지도** (숫자 없음, §4.2) |
| `quests` | list **3** | 수주 의뢰 (§4.1) |
| `allies` | list 8 | 동행 명단 |

> **`bombs`(숫자)와 `items`(이름)는 이중 장부가 아니다.** 다른 장부다 — 던질 재고의 수량은
> 시스템, 무엇을 만들었나는 서사. 판정 보정이 읽는 건 `bombs`뿐.

### 공방 설비 (party 탭 items — allow 금지)
`cauldron` 1~5 · `library` 0~5 · `storage` 0~5 · `garden` 0~5. 지갑은 `cole`.

**설비 개편 (2026-09-06, 유저 제보 "5단밖에 없는데 허전하고 쉬워 보인다 — 올리면 뭐가 달라지는지 알 수 없다").**
진단: 넷 다 보이지 않는 판정 보정(+N)뿐이라 사도 체감이 없고, 메인은 상태 블록에 설비 줄이 없어 서고가
5단이든 0단이든 같은 공방을 썼다. 값은 안 쌌다(4종 5단 합계 약 11만 콜) — 쉬워 보인 건 뜻이 안 보여서다.
원칙: **단마다 열리는 것이 있어야 하고, 그게 탭·상태 블록·지시문 세 곳에 다 보여야 한다.**

| 설비 | 판정 보정 (그대로) | 새로 열리는 것 | 선행 조건 (`requires`) |
|---|---|---|---|
| 가마 | 조합 +2/단 | 3단 미만이면 **비전** 조합 -6 · 2단 이하 고장 소동 | 4단 중급(평판>350), 5단 상급(>600) |
| 서고 | 탐사 +1/단 | **고급 레시피 2단·비전 4단부터** 배울 수 있다 (recipes desc + 지시문 + 미달 시 조합 -4/-6) | 3단 기초(>150), 5단 중급(>350) |
| 보관고 | — | **소재 용량** `mat_cap` 10→20→35→55→75→**99** (유저: 널널하게). 넘치면 `overflow` 조건 이벤트가 치울 때까지 매 턴 "상했다" | 없음 — 돈만 |
| 약초밭 | 채집 +1/단 | 🌙 하루 마무리마다 **아침 수확** (1~2단 1종·3~4단 2종·5단 3종) | 4단부터 기초(>150) |

- 용량은 목록 `maxItems`가 숫자만 받아 식이 안 되므로 **파생 `mat_cap` + 넘침 이벤트**로 만든다.
  `materials.maxItems`는 99로 올려 5단 용량과 맞춘다 (⚠ 99종이면 보조 계약표·상태창에 그대로 실린다 — 토큰 비용은 유저가 감수).
  옛 랜덤 `spoil`(보관고≤1·8종↑)은 지웠다 — 체감이 없던 자리.
- 수확은 효과에 조건이 없어 "밭이 있을 때만"을 직접 못 쓴다 → 🌙 효과가 `harvest_due = garden`을 적고,
  `garden_harvest` 조건 이벤트(`harvest_due > 0`)가 다음 아침 통지로 바꾼 뒤 0으로 되돌린다. 대리 정산(dayClose)으로 넘긴 밤도 같은 길.
- 탭 항목마다 `note`("다음 단에서 뭐가 달라지나")와 `requiresLabel`. 상태 블록에 `설비:` 한 줄, 지시문 `workshop`(always)이
  "새 레시피는 서고 단수를 보고 미달이면 아직 읽어낼 수 없다"를 메인에 건다.
- ⚠ 상태 블록은 변수 `format`을 안 입힌다 — 단위(단)는 템플릿에 직접 쓴다.

### 조합서 탭 (컬렉션) + 진열대 (2026-09-06, 유저 요청)
**조합서** — 공방 패널 셋째 탭, 템플릿. 도감 `RECIPE_BOOK` **104종**(폭탄 16·약품 16·중간재 16·도구 32·음식 16·비전 8)을
로어북 상세 항목(공격·회복·중간재·음식)에서 굽는다. [이름, **서고 단수 0~5**, 등급(기초/고급/비전 = synth_tier 어휘), 효과, 필요 소재].
2차(2026-09-06, 유저 제보 "탭마다 서고 1~5단 올라가는 느낌이 적다 · 요리 심심 · 일자 스크롤 압박"):
- **서고 단수가 진행 사다리** — 분야마다 0~5단에 항목이 깔리고, 탭 안이 "처음부터 / 서고 1단 … 5단" 묶음으로 나뉜다.
  묶음 머리가 서고 미달이면 잠김(shut)·🔒, 항목 자물쇠는 "🔒 서고 N단". 조합 벌점 문턱도 고급 3단·비전 5단으로 옮김.
- **도구 5종 × (기본 + 1~5단 업그레이드)** = 30 + 나침반·다우징 로드. 로어북엔 이름만 있어 사슬은 창작 — 앞 단계 도구가 재료,
  k단 = 격 k 지형용(튼튼한→강철→마나→별의→용린). 요리 16종(쿠키·잼·보존식·도시락·벌꿀 술·축제 과자·약초 스튜·로로나식 파이·별빛 케이크).
  로어북에서 추가: 연막탄·독병·메테오 봄·글로브·상태 치료약·영양제·둔켈하이트 영약·마나/현자의 촉매·혜성석 촉매·고대 파편 복원.
- **분야 탭은 CSS만으로** — 라디오 6개 + 라벨 + `:checked ~ .abk-pN`. 패널은 우리 화면이라 input이 산다. ⚠ 다시 그릴 때(상태 변화마다)
  첫 탭으로 돌아온다 — 엔진이 템플릿 안 UI 상태를 기억하지 않는다. 라디오 id는 `abk-{uid}-N`(uid='scg').
- 전용 패널은 못 만든다 — `party`는 단수. 탭 안 탭이 그 대답.
- recipes desc 이름표에 서고 단수를 괄호로 싣는다("레헤른(1)") — 보조가 문턱을 맞춘다. 104종이라 desc ≈ 500t.
- 해금은 `has(recipes,'이름')`, 소재 보유는 `has(materials,'이름')` — 패널 템플릿 `{…}`는 아무 식이나 계산한다.
  **완전일치**라 recipes desc에 도감 이름표(카테고리별)를 싣고, materials desc는 "이름만, 수량·수식어 없이".
- 미해금은 흐리게 + "미습득"/"🔒 서고 2단·4단"(TIER_LOCK). 툴팁은 `title`(패널은 우리 iframe에 직접 꽂혀 살아남는다).
  진행도 "n / 58 · 서고 k단". 도감 밖 창작 레시피는 목록에만 남고 책에는 안 뜬다.
- 템플릿 ≈70KB — 스키마(⚙simcore 항목)만 커진다. 그 항목은 활성화되지 않으니 토큰엔 안 잡히고, recipes desc의 이름표만 보조 계약표에 실린다.

**진열대** — 다섯째 설비 `display` 0~5 → 진열 칸 `shelf_cap` 3/6/9/12/15/18. 목록 `shelf` 항목 "이름 @+팔릴날 가격".
- 의뢰 기한과 같은 기계: `@+N`이 절대값으로 굳고, onTurn이 **합계 적기 → expire → 줄어든 값 = 매출 → cole 가산**.
  떨어진 것이 곧 팔린 것이라 돈과 물건이 어긋날 수 없고, 보조 델타(5단계) 뒤라 도로 거둔 것은 합계에 이미 없어 매출로 안 잡힌다.
- 팔릴 날·가격은 보조가 적는다(desc 규칙: 기초 1~3·고급 3~6·비전 5~10일, 고품질·평판 600↑ 하루 빠르게, 가격은 상점 밴드).
  수요 곡선은 시스템이 못 굴린다 — 한계로 기록.
- `shelf_sale`(매출 통지 + 평판 +1)·`shelf_over`(칸 넘침) 조건 이벤트, 지시문 `shelf_dir`("지어내 팔지 마라 — 통지가 왔을 때만"),
  상태 블록 "진열대(n/cap): …(N일)" 줄. 통지는 값을 못 실으니 액수는 소지금 변화로 드러난다.

**씨앗 상사 · 미끼 상점 (2026-09-06, 유저 "상점이 없긴 하네 — 미끼상점 씨앗상사").** 상점 4곳 = 상한.
- **씨앗 상사** `seeds` 🌱 (왕도·지방 도시·프리겐) — buyTo materials. 씨앗은 소재로 오고, **심으면 `field` 목록 "작물 @+익는날"**로 옮긴다
  (의뢰·진열과 같은 기한 기계). 익는 날 `(오늘)`로 보이고 → 거두면 소재, 안 거두면 다음 날 expire로 시든다.
  `field_cap = garden * 2`(0단은 심을 데가 없다 — 약초밭 설비가 씨앗의 입구), `field_over` 넘침 통지, `field_dir` 지시문("앞당겨 거두지 마라").
  옛 "🌙마다 단수만큼 약초가 난다" 수확은 **아침 밭 확인**(심은 게 있을 때만)으로 교체 — 공짜 소재가 사라지고 씨앗 경제가 생긴다.
  이름 규약: "X 씨앗"/"X 모종" → 작물 X (guide가 못박는다).
- **미끼 상점** `bait` 🎣 (왕도 어시장·강가·해안) — 미끼 4종(지렁이/반짝이/향/마나)을 정확한 이름으로. 물가(강가·폭포/해안)에서
  미끼가 있으면 **채집 판정 +3**(`has(materials,…)`), `bait_dir` 지시문이 "쓴 미끼 하나를 빼라". 생선·조개는 sellFrom으로 되판다.
- **낱말 자동 무장** (v1.7.7, 2026-09-06, 유저 "버튼 안 눌러도 채집·조합·납품·교전이 되게 못 하나"). 판정 액션은 버튼이 유일한 통로였다 → `actions[].keywords`: 유저 글에 낱말이 있으면 input 훅에서 그 액션을 무장(이미 무장이면 그대로, when·쿨다운 그대로). 13개 액션 전부 활용형 낱말("조합하/조합한다" — 어간만 두면 "조합서"에 오발, "조합한다"는 "조합하"를 안 품는다: 음절 단위).
- **이동 액션 5** (2026-09-06, 실기 제보 "상점 패널이 안 보인다" → 상점 버튼은 when이 열릴 때만 뜨는데 시작이 공방이라 넷 다 닫힘). 🏠 공방·🏙 왕도·🌑 뒷골목(평판 100 또는 단서)·🌿 들판·🌊 강가 — 이미 거기면 숨는다. 위치 set + 이동 시간 skip_min. 파생 `shops_here`(상점 when과 같은 표)가 상태창 지금 그룹·상태 블록에서 "여기 가게: 없음 — 왕도로 가면 열린다"를 말한다. 액션 13개.
- **에셋 팩 켬** (2026-09-06, 유저가 카드 Image Command Instructions 제공). 팩 3: emotion(여성 45+남성 3 × status 61, fallback default) · nsfw(여성 45 × 장면 31, when nsfw_on — /수위 0으로 끔) · plain(기본 전용 13 × default). sep _, 포맷 <img="{name}">, by main(원본이 등장·주목·발화마다 인물별 1장), verify false(얼헌과 같은 이유 — 이 환경은 이름 목록 대조 불가). 메인 주입 ≈ 900t — 원본 always-on 지시 블록과 비슷한 크기라 순증은 아니다. usage에 CBS {{user}}를 쓰지 않는다(플러그인 주입문에 CBS가 먹는지 불명 — 주인공(유저)로 풀어 쓴다).
- **시세 + 특산 (2026-09-06, 유저 "지역마다 계절·날씨 특수 재료·포션, 시장경제 이벤트로 값이 오르내리게").**
  · 플러그인 v1.7.8 `shops[].priceMul`(식 하나 또는 { 카테고리: 식, '*': 기본·매입 }) — 원가는 보조가 밴드 안에서, 배율은 상태가. 화면·결제·매입이 같은 배율.
  · 봇: `market_state` enum(평시/약초 풍년/약초 품귀/광석 품귀/상단 도착/흉년/축제 특수, allow 밖) + `market_until`(경과일). 랜덤 사건 5종이 세우고
    (caravan·herb_glut(비)·herb_short(겨울/눈)·ore_short·famine(가을·겨울), 전부 평시일 때만·쿨다운) onTurn이 기한에 평시로. 축제 당일은 축제 특수 이틀.
    상점 4곳 priceMul: 상점가 소재 0.6~1.6/식재료 흉년 1.8/'*'(완성품 매입) 축제 1.3 · 뒷골목 광석 품귀 1.5 · 씨앗 흉년>봄 0.8>겨울 1.5 · 미끼 비 0.8·생선 비 0.7.
    ⚠ 삼항 사슬은 앞이 이긴다 — 흉년을 계절보다 앞에 (테스트에서 잡힘). 지시문 `market_dir`(평시 아닐 때), 상태창 지금 그룹·상태 블록 "시세".
  · 특산: `SPECIALS` 12지형 × 4계절(48 지시문, 한 번에 하나) + `WEATHER_SPECIALS` 6(격 1↑에서 날씨별 한 줄). 로어북엔 계절 층이 없어 창작.
    이름은 계절 한정 레시피 8종과 짝(꽃맞이 향수·냉각 물약·수확제 잼·설화차·서리 폭탄·봄 이슬 증류수·반딧불 램프·혜성 가루 정제) → 도감 112종.
  · 초반 프롬프트 ≈ 3.1K t(이미지 규약 0.9K 포함), 지시문 83개 중 상시 2.
- **의뢰판 (2026-09-06, 유저 "의뢰가 게시판에 붙어 있으니 수주가 애매하다 — 댓글로 받나 서사로 받나" → "게시판 종속이 문제, 퀘스트보드를 따로 만들고 수락·취소 버튼").**
  실사: 메인은 게시판 제목 2개+헤드라인만 받고(원문 금지) 댓글 되울림도 제목 30자+댓글 80자라 보수·기한이 메인에 안 갔다. 서사로 받으면 메인이 보수를 지어내고 보조는 요약을 보고 적어 어긋난다.
  · 플러그인 v1.7.9 `questBoard` (core/quest.js, 게임 패널 6호): 보조가 게시(첫 게시 피기백 → minOffers 아래+refillEvery 보충, [새로고침] 교체), [수락]은 format대로 listVar에 push(@+N 굳힘 — 패널 삽입은 resolveRelativeExpiry를 안 지나서 모듈이 직접), [취소]는 원문 remove + cancel 효과(두 번 누르기). 통지 한 줄에 의뢰인·제목·보수·기한·내용.
  · 봇: `questBoard` — quests 형식 그대로("{client} · {title} ({grade}) @+{days} +{pay}"), 등급 심부름/기초/필드/위험/중대 = 보조 안내의 보수 밴드, days [2,14], postDays [3,8], 6/2/3, cancel 평판 -3 -floor(pay/1000) (min 1), when area_tier == 0.
    벽보(board)에서 '의뢰' 칸을 뗐다(topics·guide·categories) — 벽보는 의뢰를 소문으로만. 지시문 board_quest → "의뢰판 공고는 버튼으로 받는다, 직접 부탁만 대화로(넷을 밝혀라)". 보조 guide에 같은 줄. 기존 기한(expire elapsed)·정산(quest_pay)·납품 판정은 손대지 않았다 — 항목 형식이 같으니 그대로 돈다 (테스트로 확인).
  · 규칙 하나로 정리: **벽보 의뢰는 버튼, 사람이 직접 부탁하는 의뢰는 서사.**
- **첫 턴 순간이동 (2026-09-06, 첫 실기 — 라이자 일행이 들판에서 발견하는 도입부 뒤 "하스가 눈을 뜹니다" → 뒷골목 셋방 공방에서 깨어남).** 뿌리: 상태 블록이 첫 턴부터 "지금: 공방 / 공방 「이름 없는 공방」 — 어느 뒷골목의 셋방 · 스승 없음 / 설비: 가마 1단·서고 0단"을 말했고 설정 지시가 "공방을 확정하라"고 밀었다 — 메인은 도입부보다 상태를 믿었다(출력의 셋방·빈 선반·무쇠 가마·스승 없음·이름 모를 풀·맑은 물이 전부 초기값 그대로).
  · 고침: location 초기값 왕도 주변 들판 · 시스템 래치 `settled`(onTurn: settled or location == 공방, allow 밖) · 정착 전엔 상태 블록이 "공방: 아직 없다"만 말하고 설비 줄 비움(템플릿 식 `{settled ? … : …}` — promptState도 식이 된다) · workshop 지시문 when settled + unsettled 지시문 · 설정 지시 "도입부 자리·시간에서 이어라, 공방은 앞으로 어디에 자리 잡을지만 정해지면 된다" · setup guide "아직 안 갔으면 절대 공방이 아니다".
  · 원칙: **초기값은 첫 장면의 사실이어야 한다.** 밑천(레시피·소재)은 남겨도 되지만 "어디 있나·무엇이 곁에 있나"는 도입부가 정한다. 설정 턴엔 지시문이 안 실린다(setup.ai.instruction만) — 정착 전 안내는 그 다음 턴부터.
- **스킨 (2026-09-06, 유저 "상태창·패널 CSS가 아쉽다 — 아틀리에풍 밝고 귀여운 느낌").** 생성기 SKIN 팔레트(크림 종이·코코아 글자·살구/꿀/민트/하늘/라벤더, 둥근 모서리·점선·2px 아래 그림자 버튼) 하나를 세 곳이 나눠 쓴다: statusUI.customCSS(테마 clean으로, .sim-* 전부 덮음) · PANEL_CSS(열린 패널 것만 주입되므로 party·calendar·board·questBoard·messenger·shops 4 전부 같은 문자열; #sc-game 배경막까지 — scopeCss는 prefix로 시작하는 선택자를 그대로 둔다) · 조합서/지도 템플릿 인라인 style. 기본 스킨이 색을 박은 클래스는 전부 덮어야 한다(배경만 바꾸면 밝은 글자가 사라진다). 플러그인 무접촉.
- **세금 (2026-09-06, 유저 "돈 나갈 구석이 애매하니 세금이라도").** 월세는 계열마다 "자기 공방" 서사와 부딪혀 배제, 세금은 왕도 장인이면 누구나.
  · 두 겹: 공방세 100 + 설비 단수 합×60 (시작 160 → 만렙 1,600) + 상거래세 = 이달 진열 매출(sales_month, onTurn이 shelf_sold 누적) 1할. 의뢰 보수엔 안 붙는다(초반 숨통·명분).
  · 사건 셋(매달 1일, 래치 tax_seen=연*100+월, 시작 140004라 첫 세금은 5월): tax_paid(cole -= tax_due) / tax_short(잔고 0·평판 -10·체납 1) / tax_seize(두 달째: 진열대 -1·체납 정산). 제때 내면 체납 풀림. 전부 시스템, allow 밖.
  · 지시문 tax_soon(28일부터, 세액 포함)·tax_arrears_dir · 달력 표식 세금날 · 상태창 공방 그룹(tax_due·sales_month·체납) · 상태 블록 설비 줄에 "다음 세금".
  · 테스트 함정: shelf를 vars에 직접 넣으면 @+N이 안 굳어 영영 안 팔린다 — 보조 델타(add)로 넣어야 한다.
- **특수연금 (2026-09-06, 유저 "자기만의 레시피·포션을 창조해 조합서에 등록 — 전부·무제한은 아니고 특수연금으로 따로").** 플러그인 무접촉, 생성기만.
  · 액션 act_invent(🔮, 낱말 특수연금/실험/새 레시피; 공방·서고 2단·전투 아님; 4시간·체력 -24) → 판정 invent(vs 18, mod 서고+가마+숙련/10+단서≤3): 발명(장부+고품질)/성공(장부+완성품)/불안정(완성품만, 재현 불가)/실패(재료만 소실)/사고(체력 -15). 재료 3~4종은 판정과 무관하게 소모(액션 inject).
  · 장부 inventions(list 12칸, "이름 ← 재료 · 재료 · 재료 (등급, 효과)"; allow) — 뇌절 3중 방지(재료는 실험 당시 materials 이름만 · 이름은 도감·장부와 겹치지 않게 · 등급은 서고 단수 이하)는 보조 규칙(desc·guide), 상한 12가 백스톱. recipes desc는 "도감 밖 이름은 여기 올리지 마라"로 바꿈.
  · 조합서 7번째 탭 "특수"({inventions:tags} 칩, N/12, 서고 2단 잠금) · 지시문 invent_dir(장부 재현 규칙 — 적힌 재료로만, 도감 로어북 게이트와 같은 역할)·invent_full(12칸) · 상태창 소지 그룹.
  · 한계: 어느 재료를 넣었는지 시스템은 모른다 — 소모는 보조 몫(채집·조합과 같은 수준).
- **조합 재료 게이트 + 약품 확장 (2026-09-06, 유저 "재료 부족하면 거절되나 · 조합서 밖 의뢰는 서사로 · 약품 18개는 적다").**
  실사: 조합 버튼 조건은 not fight_on뿐, 조합서 필요 소재는 패널 전용이라 메인이 모른다 → 재료 없이도 어거지로 됐다.
  · 로어북식 게이트 (유저 선택 — 아는 레시피 전부를 지시문으로 실으면 배울수록 매턴 비용): 생성기가 `조합서.json` 사이드카를 쓰고 convert-lorebook.js가 레시피마다 **키워드 활성 로어북 항목 130개**(폴더 "조합서 (심코어)", ≈71t/벌)를 굽는다 — 이름이 채팅에 나올 때만 효과·필요 소재·규칙 한 벌. 한 글자 이름(약·빵)은 "약을 만/약을 빚/약 조합/약을 지어" 어구 키.
  · 규칙: workshop 지시문에 "조합서에 있는 것은 적힌 소재로만, 없으면 판정과 상관없이 멈춘다(같은 계열 대체 한 가지), 조합서 밖은 서사에" + 보조 guide "멈췄으면 완성품·소재 손대지 마라". 판정 자체는 여전히 굴러간다 — 완전 차단은 "만들려는 것" 변수가 없어 플러그인 몫(보류).
  · 약품 18 → 36 (범용·종합: 감기약·소독액·해열제·진통 연고·위장약·안약·벌레 물림 연고·수면제·각성제·해독 연고·체력 회복약·마나 회복약·만능 해독제·방어 연고·강화 물약·정신 안정제·재생약·만병통치약) → 도감 130. 의뢰판 guide: 만들어 달라는 의뢰는 도감 이름으로(아는 것 위주·셋에 하나는 서고 한 단 위·도감 밖은 드물게 옛 문헌 이름).
- **품질 개편 (2026-09-06, 유저 "품질 등급 어디서 보나 · 값·효과에 얽히나").** 실사: 판정 라벨·이름 접두어(고품질/조잡한)뿐이고 last_quality는 저장만·아무 데도 안 실림, 납품·가격은 품질을 안 읽었다.
  · 상태창 연금술사 그룹 + 상태 블록 "직전 조합 {last_quality}" · 납품 mod에 직전 품질(걸작 +4·상품 +2·조잡 -3·실패 -5 — 그 물건이 아니라 마지막 조합이라 납품 직전에 만들면 정확)
  · 가격은 문장 규칙(보조 guide·items·shelf desc): 고품질=밴드 상단·조잡=하단·접두어 없음=중간, 진열 팔릴 날 고품질 -1·조잡 +1. 목록 항목에 품질 숫자를 못 넣어 기계 연동은 플러그인 몫으로 남김.
- 남은 것: 도구 사슬이 숫자로는 무의미(`count(tools)`) · 월세 없음(돈이 남아돈다) · 실기 0회 · 에셋 이름 목록·정규식 export 미수령.

### 시간

**축제 (2026-09-06, 유저 질문 "달력 있는 김에 축제·명절 일정도").** 로어북엔 이름 붙은 축제가 없다 — "축제 물품·노점·마을 축제"
일반 언급과 백색 혜성(150년 전 소멸) 설정뿐이라 원작 이식이 아니라 계절·로어에서 지은 것. `FESTIVALS` 한 표(생성기)에서
**달력 표식 + 사흘 전 준비 지시문(`prep_*`, D-3~D) + 당일 이벤트(`fest_*`, 연 1회)**가 함께 나온다 — 표식만 있고 아무 일도
안 하던 달력을 살아 있게. 래치 `fest_seen = year*100+month`(한 달에 하나, 일은 4 이상이라 준비 창이 달을 안 넘는다).

| 월/일 | 축제 | 걸리는 시스템 |
|---|---|---|
| 4/7 | 꽃맞이제 | 향·꽃 소재, 쿠키·도시락 주문 (시작 6일 뒤 — 첫 축제가 온보딩) |
| 6/21 | 한여름 별시장 | 밤장 — 램프·얼음 폭탄·사탕, 뜨내기 상인 희귀 소재 |
| 8/8 | 왕도 건국제 | 협회 대량 주문(불꽃 폭탄·붕대·휴대식량), 뒷골목 조용 |
| 9/15 | 수확제 | 카페 대목 — 파이·잼·벌꿀 술, 과일·꿀 넘침 |
| 11/11 | 첫눈 등불제 | 램프·기름·약초차·스튜, 약 의뢰 증가 |
| 12/24 | 혜성 관측일 | **clues +1** (마나 출렁임 — 시나리오와 연결) |

효과는 혜성 관측일만 (단서). 나머지는 서사 유도 — 축제가 시스템 수치를 공짜로 주면 매년 같은 날 돈이 떨어진다.
시작 **1400년 4월 1일 08:00** (중세 판타지 감각, 유저 지정 2026-09-06 — 원래 1000년). 날짜 표기 `YYYY년 M월 D일`, `year_no = year - 1399`.
상태창 첫 그룹 **지금**: 날짜·요일·시각·계절·날씨·위치 (date/clock/weekday/season은 time.expose 이름을 statusUI에 그대로 쓴다).
날씨 `weather` enum(맑음/흐림/비/바람/안개/눈)은 보조가 서사를 따라 적는다 — 시스템 기상은 없다. 랜덤 이벤트는 `squall`로 개명.

`time`: gregorian, start `0001-04-01 08:00`, `expose`에 **`year` 포함**(원본 "년도: 1"),
format `M월 D일` / `HH:mm`, weekdays 월~일, seasons 봄여름가을겨울.
`skip_day`(0~3650, **캡 금지**) · `skip_min`(0~1440).

---

## 4. 판정

| id | roll | mod | vs | 등급 |
|---|---|---|---|---|
| `gather` | d20 | `floor(renown/100) + count(tools) + (stamina<30 ? -3 : 0)` | `8 + area_tier*2` | 만재 / 성과 / 빈손에 가깝다 / 헛수고+사고 |
| `survey` | d20 | `floor(renown/80) + library` | `10 + area_tier*2` | 발견(clues+1, areas 추가) / 흔적 / 허탕 / 사고 |
| `synth` | d20 | **§4.3** | `synth_tier` 사슬 (10/15/20) | 걸작 / 상품 / 보통 / 조잡 / 실패 |
| `battle` | d20 | `floor(renown/80) + min(bombs,3)` | `10 + foe_tier*2` | **fight** — 등급마다 `gain` |
| `guard` | d20 | `floor(stamina/20)` | `10 + foe_tier*2` | 회피 / 스침 / 피격 / 직격 (stamina 차감) |

- `synth` 실패 등급의 `inject`에 **로어북 149의 실패 연출**을 그대로 싣는다 (가마가 뻥,
  거품, 검댕, 이상한 냄새 — 평범한 레시피는 희극, 위험한 레시피만 진짜 사고).
- `battle.fight`: `gauge: "25 + foe_tier * 20"` · `reply: 'guard'` · `lose.when: 'stamina <= 0'`.
  **결착은 게이지에서 나온다** — 한 응답에 전투를 끝내지 않는다.

### 4.1 의뢰 — 서사가 계약하고, 시스템이 지급한다

**항목 형식** (`quests`, maxItems **3**):

```
별의 고치 카페 · 감기약 3병 (중급) @+5 +800
```

기한 `@+5`와 보상 `+800`이 한 줄에 공존한다 — `itemValue()`가 **기한 태그를 먼저 떼고
맨 끝 숫자를 잡기** 때문이다 ([core/expr.js](../core/expr.js#L49)). 보상은 반드시 맨 끝.
`@+5`는 다음 전송에 절대 기한(`@455`)으로 굳고, 화면·프롬프트에는 `(5일)`로 환산돼 나간다.

**세 단계**

1. **수주** — 서사가 "이걸 해 주면 800콜"이라고 정하고, 보조가 그대로 목록에 박는다.
   **여기가 유일한 창작 지점**이라 로어북 150의 보상대(심부름 50~200 … 중대사 5,000~15,000)를
   `updater.guide`에 싣는다.
2. **꽉 참** — `directives { when: "count(quests) >= 3", text: "의뢰 수첩이 가득 찼다.
   새 의뢰는 정중히 거절하거나 하나를 끝낸 뒤로 미루게 하라." }`
   > 지시문이 **꼭 필요하다.** 4번째는 엔진이 `slice(0, maxItems)`로 **조용히 잘라 버린다**
   > (engine.js:390) — 서사에선 받았는데 목록엔 없는 유령 의뢰가 된다. 거절을 서사에 시켜야 한다.
3. **납품·정산** — 보조가 `quest_pay`에 **그 항목에 박힌 숫자를 옮겨 적고** 항목을 `remove`.
   이벤트가 지급한다:
   ```
   events: { when: "quest_pay > 0",
             effects: [ cole = cole + quest_pay, quest_pay = 0 ],
             notify: "약속된 보수를 받았다." }
   ```
   **왜 액션이 아니라 이벤트인가**: 액션 효과는 *전송 단계*(장면이 써지기 전)에 돌고,
   보조 델타는 *응답 단계*에 온다. 이벤트는 보조 델타 **뒤**라서 같은 턴에 정산된다.
   `📮 납품` 버튼은 `deliver` 판정으로 **평판만** 건드린다 — 돈은 계약액(결정적),
   평판은 솜씨(변동). 의뢰인은 약속한 만큼 준다.

`quest_pay.desc` = "목록에 박힌 숫자를 그대로 옮겨 적어라. 새로 정하지 마라." + maxGain 15000.

**4. 기한 — 미리 보여 주고, 넘기면 알린다**

*사전 경고는 공짜다.* 기한 `@455`는 화면·프롬프트로 나갈 때 **`(3일)` / `(오늘)` / `(지남)`으로
자동 환산**된다 (v1.7.1). 환산되는 곳에 **상태창과 지시문·`promptState.template`이 다 들어 있어서**,
지시문 한 줄만 깔면 메인 모델이 매 턴 남은 날짜를 글자로 읽는다:

```
directives { when: "count(quests) > 0",
             text: "수주 중인 의뢰: {quests} — 기한이 임박한 것은 서사에서 압박으로 드러내라." }
```

*만료 통지는 세 줄로 만든다.* `onTurn`의 `expire`는 항목을 **조용히** 떨어뜨리므로,
갯수 차이로 잡아낸다 (`applySets`가 효과를 순차 적용해 앞 결과를 뒤가 본다 —
[engine.js:433](../core/engine.js#L433)):

```
onTurn (순서 그대로):
  1. { list: 'quests', expire: 'elapsed' }
  2. quest_lost = max(quest_n - count(quests) - (quest_pay > 0 ? 1 : 0), 0)
  3. quest_n    = count(quests)                        ← 다음 턴 비교용 기준

events { when: "quest_lost > 0",
         effects: [ renown = max(renown - 15 * quest_lost, 1), quest_lost = 0 ],
         notify: "기한을 넘긴 의뢰가 취소됐다. 의뢰인의 말을 전해 들은 참이다." }
```

> **처음에 `expire: 'elapsed - 2'`로 2일 유예를 두려다 접었다 (실측).** expire 식은 만료 기준이자
> **`@+N`을 굳히는 시계이자 화면 환산의 기준**이라 셋이 같은 식이다 — 오프셋이 세 곳에서 똑같이
> 상쇄돼 `'elapsed'`와 결과가 **완전히 같다**. 유예를 두려면 시계를 갈라야 하는데 그런 통로가 없다.
> 지금 동작: 기한 당일까지 살아 있고 `(오늘)`로 보이다가, 다음 날 떨어지면서 통지가 뜬다.
> `(지남)` 상태는 자동 만료 목록에선 볼 일이 없다 (틱이 화면보다 먼저 돈다).

`quest_pay > 0` 항을 빼는 이유: 납품으로 빠진 것까지 만료로 세면 안 된다 (보조 델타는 5단계,
틱은 6단계라 같은 턴에 이미 빠져 있다). 효과가 `quest_lost`를 0으로 되돌리므로 **조건 자기해제**다.
`quest_n`·`quest_lost`는 카운터라 **allow 금지**. `calendar.list = 'quests'`로 달력에도 선다.

### 4.2 채집지 난이도 — 스키마에 미리 박는다

먼저 버린 안: `areas`에 `"버려진 갱도 3"`처럼 숫자를 박고 `sum(areas, location)`으로 읽는 것.
문법상 되지만 매칭이 **`항목.includes(필터)`**([core/expr.js](../core/expr.js#L246))라
*항목이 위치를 품어야* 하는데, 자유 텍스트 `location`("잊혀진 평원 깊은 곳")은
`"잊혀진 평원 1"`에 안 걸리고 **안 걸리면 조용히 0**이 된다.

**확정안: `location`을 enum으로 만들고 `area_tier`를 파생으로 뽑는다.**
난이도를 매번 정하지 않는다 — 표가 스키마에 박혀 있어 영영 안 흔들린다.

로어북이 이미 **고유명사가 아니라 지형으로** 써 놨다 (15 던전과 채집지 · 58 지역별 소재 배치).
그래서 enum도 지형·권역으로 잡는다 — 어느 계열에서 시작하든 그대로 쓰인다.

| `location` (enum) | `area_tier` |
|---|---|
| 공방 · 왕도 · 별의 고치 카페 · 왕도 뒷골목/부두 · 지방 도시 | **0** (채집 불가) |
| 왕도 주변 들판 · 프리겐/시골 | 1 |
| 숲 · 꽃밭/초원 · 강가/폭포 · 해안 | 2 |
| 습지/늪 · 광산/동굴 | 3 |
| 설산 능선 · 사막 · 유적/마나 이상 지대 | 4 |
| 세계의 끝 | 5 |

- `area_tier`는 **파생**이라 AI도 규칙도 못 건드린다. 보조는 `location` enum만 고른다
  (enum은 절대값 적용이라 델타 함정도 없다).
- 서사는 원하는 이름을 그대로 쓴다 — "잊혀진 평원 깊은 곳"이라 부르든 enum은 `숲`이면 된다.
- `⛏ 채집`·`🔍 탐사`는 `when: 'area_tier >= 1'`로 잠근다 (도시에서 채집 버튼이 안 눌린다).
- 어디서 뭐가 나오는지는 **로어북 58이 그대로 담당한다** — 심코어는 몇 칸인지만 정한다.
  `directives { when: "area_tier >= 1", text: "지금은 {location}이다. 여기서 날 만한 소재만 내라." }`
- `areas` 목록은 숫자 없는 **순수 지도**(가 본 곳의 도감)로 남는다.

### 4.3 품질 — 판정이 곧 가챠

**품질 변수를 아이템마다 달지 않는다.** 조합 판정 자체가 가챠고, 숙련도가 확률을 밀어 올린다.

```
mod = floor( <synth_cat에 해당하는 숙련> / 12 )   ← 0~8   분야 숙련
    + cauldron * 2                                ← 0~10  공방 도구 수준
    + floor(renown / 150)                         ← 0~6   평판이 거든다
    + (stamina < 30 ? -3 : 0)
vs  = 10 / 15 / 20                                ← 레시피 자체 난이도(synth_tier)
```

**난이도를 3단으로 줄인다 — 로어북 문단 구조가 곧 등급이기 때문이다.**
로어북 45~48이 이미 `Common ~` / `Advanced ~` 두 문단으로 갈라 놨고, 43이 `희귀와 고위`다.
5단으로 쪼개면 경계가 애매해져 매번 달라지지만, **"그 아이템이 로어북 어느 문단에 있나"**는
흔들릴 여지가 없다. `synth_tier.desc`에 그 대응을 그대로 박는다.

| `synth_tier` | 로어북 근거 | vs |
|---|---|---|
| 기초 | `Common attack/recovery/support items` 문단 (프람·레헤른·힐링 살브·중화제) | 10 |
| 고급 | `Advanced ~` 문단 (드라코 봄·테라 봄·힐링 클라우드) | 15 |
| 비전 | 43 `희귀와 고위 연금술` (현자의 돌·엘릭서·에테르널 크리스탈) | 20 |

숙련 0·가마 1·평판 50이면 mod +2라 비전(20)은 d20으로도 못 넘는다. 숙련 100·가마 5·평판 1000이면
+24라 비전도 안정적으로 넘긴다 — **"도전은 되지만 위험하다"**는 로어북 149의 규약 그대로다.

`synth_cat`·`synth_tier` 둘 다 **보조가 서사에서 읽어 기록한다**(allow). 유저가 매번 칠 필요가
없고, 판단 기준표가 `desc`에 고정돼 있어 AI 재량이 아니다. `cmd`(`/분야`·`/난이도`)는 덮어쓰기용으로만 남긴다.

**등급 = 품질**: 걸작(`total >= vs+8`) / 상품(`>= vs+3`) / 보통(`>= vs`) / 조잡(`>= vs-4`) / 실패.
각 등급 effects가 `last_quality`를 세우고 **그 분야 숙련만** 올린다
(걸작 +3 / 상품 +2 / 보통 +1 / 조잡 +1 / 실패 +1 — 실패해도 배운다).

> 분야가 6종이라 "어느 숙련을 올릴지"는 `set`으로 못 고른다. 등급마다 6줄을 깔고
> 각각 `expr: "synth_cat == '폭탄' ? sk_bomb + 3 : sk_bomb"` 꼴로 쓴다 — 생성기가 찍는다.

품질은 **`last_quality` 하나**만 상태에 남는다. 그 아이템이 계속 고품질인지는 `items` 목록의
이름이 들고 간다("고품질 힐링 살브" — 원본 상태창 예시가 이미 그렇게 쓴다).

## 5. 액션

`⛏ 채집`(check gather, skip_min +240, stamina −15) · `🔍 탐사`(survey, +300, −20) ·
`🧪 조합`(synth, +180, −12) · `⚔ 교전`(battle, `when: true`) · `🏃 이탈`(fightEnd, check guard,
`when: 'fight_on'`) · `📮 납품`(renown+, quests 정리) · `😴 휴식`(skip_min +240, stamina +35) ·
`🌙 하루 마무리`(**`dayClose: true`**, skip_day +1, stamina 회복, 의뢰 만료 정리)

> `dayClose`가 있어야 **버튼을 안 눌러도 "다음 날 아침"이라고 쓰면 날짜가 넘어간다.**
> 정산은 여전히 이 액션 한 벌뿐이라 입구가 둘이어도 결과가 안 갈라진다.

## 6. 메인이 받는 통로 (`promptState.template`)

**이게 없으면 상태창은 유저만 보는 장식이다.** 얼헌 v1.5.3 실사고의 자리.

```
지금: {date}({weekday}) {clock} · {season} · {weather} · {year_no} · {location}
공방 「{atelier_name}」 — {atelier_place} · 스승 {mentor}
설비: 가마 {cauldron}단 · 서고 {library}단 · 보관고 {mat_n}/{mat_cap} · 약초밭 {garden}단
평판 {renown}({alch_tier}) · 소지금 {cole}콜 · 체력 {stamina}/100
소재: {materials}
아이템: {items} · 투척 {bombs}
의뢰: {quests}
```

`systemGuide` 끝자락에 진행 폭 앵커 — "지금 이 자리에서 벌어지는 일을 끝까지 그리고 거기서
멈춰라. 장면을 넘길지는 유저가 정한다."

---

## 7. 로어북 분담 — 뭘 빼고 뭘 남기나

원칙: **로어북 = 안 변하는 것(설정·이유·어휘), 변수 = 플레이가 남긴 흔적.**

### 반드시 뺀다 (심코어와 정면 충돌)
| # | 항목 | |
|---|---|---|
| 145 | 상태창 (always, 740t) | **본문 맨 아래에 상태 블록을 찍으라는 지시** — 심코어 상태창과 두 개가 뜬다 |
| 148 | 상태창/인벤토리 운영 규칙 | `updater.guide` + 변수 `desc`로 이관 |

### 숫자만 빼고 산문은 남긴다
| # | 항목 | 옮길 곳 |
|---|---|---|
| 149 | 연금술 난이도와 실패 연출 (650t) | 5단 구간 → `derived alch_tier` / 실패 연출 → `synth` 등급 `inject` |
| 150 | 경제와 보상 규칙 (624t) | 가격대 → `shop.bands` / 의뢰 보상대 → `updater.guide` + `cole` maxGain 15000 |
| 66 | 난이도 보상 실패 처리 (always) | 기한 → `quests`의 `@+N` + `calendar` |

### 쪼개서 옮겼다 → 원본은 지운다 (P3 ✅)
| # | 항목 | |
|---|---|---|
| 144 | npc 리스트 (always, 3,792t) | **이미 스키마 지시문 8벌로 구워졌다.** 원본을 안 지우면 이중으로 실린다 |

### 그대로 둔다
세계관(1~6) · 지역(8~16) · 세력(18~24) · 몬스터(26~35) · 아이템·소재 어휘(37~58) ·
의뢰 유형(61~65) · 캐릭터 시트(69~142) · 타임라인(147).
**전부 "안 변하는 것"이다.** 아이템·소재 목록은 서사가 이름을 고르는 어휘집이므로 남긴다 —
심코어는 몇 칸인지만 정하고 무엇인지는 정하지 않는다.

### 확인 필요
146 `퓨처플랜`이 **응답 맨 끝에 `[💡Request Profile| …]`을 요구**한다. 심코어 저장 마커도
본문 끝 고정이다. 실기에서 두 줄이 부딪히는지 확인 — 부딪히면 프로필 요청을 본문 앞으로 옮긴다.

⚠ **정규식(customscript)은 손대지 못했다.** 유저가 준 것은 로어북 export뿐이라 카드의 정규식을
볼 수 없었다. 번들은 `regex` 키를 **일부러 비워** 원본 정규식을 건드리지 않는다
(`applyBundleToChar`는 regex가 배열일 때만 덮는다). 원본에 **옛 상태창을 꾸미는 정규식**이 있다면
심코어 상태창과 겹치므로 카드에서 직접 꺼야 한다.

### 실행 — 번들 한 파일로 통째 교체 (P4 ✅)

리수에는 로어북 일괄삭제가 없다. 그래서 "하나씩 지우세요"가 아니라 **개조 번들**로 간다
(adapter v1.0.5 — 💾 세이브 페이지 `[번들 가져와 교체]`). 스키마도 로어북에 실려 함께 간다.

`node 아틀리에/convert-lorebook.js` → `아틀리에-번들.json` (455KB)

| | |
|---|---|
| 삭제 3 | npc 리스트 3,792t · 상태창 740t · 인벤토리 규칙 340t |
| 강등 4 (always → 키워드, 내용 유지) | 아이템 목록 1,195t · 소재 목록 607t · 의뢰 구조 403t · 난이도·보상 376t |
| always 유지 2 | 이방인 700t(규칙이지 어휘가 아니다) · 퓨처플랜 512t(프로필 호출 장치) |
| 첨부 | `⚙simcore` 스키마 항목 |

**로어북 always-on 8,325t → 1,212t (85% 절감).** 심코어가 되얹는 몫까지 세면:

| | 원본 | 심코어판 | |
|---|--:|--:|--:|
| 초반 | 8,325t | 1,212 + 774 = **1,986t** | −76% |
| 후반 최악 | 8,325t | 1,212 + 1,943 = **3,155t** | −62% |

여기에 원본은 **매 응답 본문에 상태 블록을 찍는 출력 비용**이 따로 있었다 — 그건 통째로 사라진다.

- 강등한 항목은 **한국어 낱말 키가 있는지 확인하고 강등한다**. 「아이템 목록」은 키가 어구뿐이라
  (`아이템 목록`, `레시피 목록`) 그냥 강등하면 한국어 채팅에서 영영 안 뜬다 — `조합·레시피·아이템`을
  보탰다. `연금술`처럼 매 턴 나오는 낱말은 넣지 않는다 (그러면 강등한 뜻이 없다).
- 변환기가 삭제·강등·유지 대상을 **원본에서 못 찾으면 출력하지 않고 죽는다.** 폴더 수·⚙simcore
  왕복·한국어 키 유무도 같이 검사한다.

### 덤으로 찾은 것 — 죽은 항목
147 `타임라인`(3,096t)은 **키도 없고 always도 아니다 → 영영 안 뜬다.** 지금까지 이 봇은 캐논
타임라인 없이 돌아온 셈이다. 번들에는 그대로 두었다(보관용). 살리려면 키에
`타임라인, 캐논, 원작, 챕터` 정도를 넣으면 되지만, 뜨는 순간 3,096t이라 권하지 않는다 —
시나리오 5막이 이미 척추를 대신한다.

---

## 8. 단계

**P1 ✅ 뼈대** — setup.ai + 변수 37·파생 6·판정 6·액션 8 + time + calendar + party 설비 탭 +
promptState + statusUI. 검증 통과 · 진단 깨끗.

**P2 ✅ 경영** — 상점 2곳 + 게시판 1판. 자체 테스트 67건 통과.

| | |
|---|---|
| `shops[market]` 왕도 상점가 | 소재/도구/식재료/서적 · 5등급 · `when: 왕도\|지방 도시` · buyTo `materials` / sellFrom `items` |
| `shops[shade]` 뒷골목 거래처 | 희귀 소재/수상한 물건 · 3등급 · `when: 왕도 뒷골목` · sellFrom `materials` (아무거나 사 준다) |
| `board` 별의 고치 게시판 | 칸 5개 `의뢰·소문·연금·거래·잡담` · 자율형 [2,3] · `hot` 5턴마다 · `when: area_tier == 0` |

- **가격 뇌절은 구조가 막는다**: 어휘 밖 등급은 거부되고 밴드 밖 가격은 클램프된다
  (실측: `조악 9999콜` → `60콜`). 밴드는 로어북 150의 가격대를 그대로 옮긴 것.
- **`board`는 단수 전용**(`shops`만 1~4개) — 그래서 카페 의뢰판과 "연금넷"을 한 판의 **칸**으로 나눴다.
  `연금` 칸이 연금술을 아는 소수가 서로 묻고 답하는 자리다. 란타르나에 인터넷은 없으므로
  **카페 벽의 손글씨 벽보**로 못박고, guide에서 줄임말·밈·"ㅋㅋ"를 금지한다.
- **자율형 [2,3]** — 반응형이면 매턴 주인공 서사가 박제돼 세계가 죽는다 (얼헌 v1.1.0 실사고).
- 게시판 「의뢰」 공고는 **아직 수주가 아니다.** 유저가 받겠다고 해야 `quests`에 오르고,
  그때 공고의 보수·기한을 그대로 옮긴다 (지시문 `board_quest`).

### 패널 결정 — 퀘스트 보드는 안 만들고, 지도는 만든다 (유저 질문, 2026-09-05)

| | 판단 | 근거 |
|---|---|---|
| 퀘스트 보드 탭 | **안 만든다** | 의뢰는 이미 세 곳에 있다 — 상태창 「의뢰」 그룹(수주분·`(N일)` 환산) · 📅 달력(기한이 날짜에 박힘) · 📋 게시판 「의뢰」 칸(수주 전 공고). 넷째는 중복이다 |
| 채집지 지도 탭 | **만든다** | `areas`가 「여정」 그룹의 평문 목록뿐이라 "어디가 얼마나 위험한가"를 한눈에 볼 자리가 없었다 |

지도는 **새 패널이 아니라 🏠 공방 패널의 둘째 탭**이다 (`party.tabs[].template` — 얼헌 서울 지도와
같은 문법). 버튼은 늘지 않는다 — 탭 하나에 fab을 달아 버튼이 둘 뜬 실기 제보 직후라 지름길도
안 달았다 (원하면 `fab: '🗺️'` 한 줄).

- **지형표(`PLACES`)에서 그대로 굽는다** — 격 0~5 사다리에 지형 17종. 표와 어긋날 수 없다.
- 가 본 곳은 `{areas:tags:지형}` 필터로 **제 칸에 꽂힌다.** 그래서 `areas` 항목 규약을
  **"이름 (지형)"** 으로 바꿨다 — 지형은 `location` enum 값 그대로 (`"버려진 갱도 (광산·동굴)"`).
  지형을 안 붙인 항목은 어느 칸에도 안 들지만 상태창 「여정」에는 그대로 남는다 (조용한 소실 없음).
- 머리에 `지금 {location} · 격 {area_tier}`. 현재 칸 강조는 **일부러 안 했다** — 새니타이저가
  `data-*`를 지우므로 CSS 속성 선택자 트릭이 상태창에선 죽는다 (render.js:552). 글자로 충분하다.
- 실측 10건: 갱도가 광산 칸에·들판 칸엔 근교+평원·갱도는 들판에 없음·미치환 0·CSS `#sc-game` 격리.

**P3 ✅ 세계** — 캐스트 맵 분할 + 시나리오 + 서신 + 에셋 뼈대. 자체 테스트 **93건 통과**.

| | |
|---|---|
| 캐스트 맵 | 지시문 8벌 (코어 1 · 적대 2 · 계열 5). 초반 562t / 최악 1,697t — 위 §1 |
| `scenario` 「잊혀진 기술」 | 5막: 이름 없는 공방 → 쓸모 있는 것 → 소문이 돈다 → 눈길 → 혜성의 자취 |
| `messenger` 「서신」 | `contactsVar: allies` · `notesVar: ally_notes` · `when: area_tier == 0` |
| `assets` | 뼈대만, **`enabled: false`로 출고** |

- **시나리오는 캐논을 따라가지 않는다.** 로어북 147 타임라인은 참고 자료지 레일이 아니고,
  봇 디스크립션도 "session continuity wins"라고 못박았다. 이 척추는 **유저 자신의 연금술 부흥**이라
  어느 계열에서 시작하든 그대로 선다. 해금은 `renown`(세상의 신용)과 `clues`(실마리)가 민다.
  실측: 문턱을 넘겨도 `minTurns` 전엔 안 넘어가고, 전환은 턴당 한 막이다.
- **서신은 단말기가 아니다.** 심부름꾼·행상인이 전해 주는 편지 — guide가 줄임말·이모티콘을 막고
  "급한 일은 편지로 오지 않는다(사람이 직접 뛰어온다)"를 못박는다. 들판에선 발신·선톡이 멈춘다.
- **에셋은 꺼진 채로 나간다.** 카드에 실린 **실제 에셋 이름을 모르는 채로 켜면 조합이 전부 대조
  실패해 이미지가 0장**이 되고, 그건 에러 없이 조용히 실패한다 (v1.4.1 사고). 카드의 에셋 이름을
  확인해 `who` 값·`sep`·`format`을 맞춘 뒤 `enabled: true`로 켠다.

작업 파일: `아틀리에/atelier-vars.js` (생성기 — **여기만 고친다**) → `아틀리에/공방-아틀리에.json` (68.8KB).

## 9. 확정된 것 / 남은 것

**확정** (유저 결정, 2026-09-05): 의뢰 = 서사 계약 + 이벤트 정산, 3개 상한 + 거절 지시문(§4.1) ·
채집지 난이도 = `area_tier` 변수(§4.2) · 품질 = 판정 가챠 + 분야 숙련 6종(§4.3).

**등장인물은 제한이 아니다.** `origin`은 always-on 캐스트 맵만 가른다 — 개별 인물 로어북
항목은 이름 키워드로 뜨므로, 어느 계열의 누구를 불러도 그대로 등장한다.
로어북 146 `퓨처플랜`의 `[💡Request Profile| …]`이 그 순간 전체 프로필을 끌어오는 통로고,
그건 심코어가 대체하지 않는다 — **그대로 남긴다.**

**추가 확정** (2026-09-05): 의뢰 기한은 `(3일)`→`(오늘)`→`(지남)` 자동 환산으로 **미리 보이고**,
2일 유예 뒤 떨어질 때 갯수 차 감지 + 이벤트로 **통지·평판 감점**(§4.1-4) ·
채집지 난이도는 `location` enum → `area_tier` **파생**으로 스키마에 못박음(§4.2) ·
레시피 난이도는 **로어북 문단 = 등급** 3단(§4.3).

> 관통하는 원칙: **매번 정하지 않는다.** 흔들리면 안 되는 난이도는 스키마(파생·enum)에 박고,
> 스키마에 못 박는 것(레시피 이름은 무한하다)은 판단 기준표를 `desc`에 고정해 재량을 없앤다.

**남은 것**
1. 소재 품질. 지금 안은 조합 *결과* 품질만 다룬다 — 좋은 소재가 보정에 들어가려면
   `mat_grade` 같은 축이 하나 더 필요하고, 그만큼 관리가 는다. P2 이후 판단.
2. `location` enum 17종의 최종 목록 — 로어북 8~16·58에서 뽑되, 실기에서 빠진 지형이 나오면 보강.
