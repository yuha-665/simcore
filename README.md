# SimCore — RisuAI 시뮬 엔진 플러그인

봇에 숫자로 굴러가는 상태를 붙이는 RisuAI 플러그인. 스키마 하나로 변수·파생·규칙·이벤트·상태창을
정의하면, 메인 모델에는 상태 블록을, 보조 모델에는 갱신 지시를 내보낸다.

## 리포 구성

| 경로 | 내용 | 상태 |
|---|---|---|
| `simcore.plugin.js` | 빌드 산출물 (리수에 임포트하는 파일) | v0.38.2 |
| `.claude/skills/simcore/` | 개발·유지보수 레퍼런스 스킬 (Claude용) | ✅ |
| `core/` | 소스 모듈 | ⚠ **미이관 — cowork 환경에서 포팅 필요** |
| `build.js` | 번들 빌드 스크립트 (`node build.js → dist/simcore.plugin.js`) | ⚠ 미이관 |
| `테스트/` | `test-*.js` — 전부 돌려 `N passed, 0 failed` 확인 | ⚠ 미이관 |

## ⚠ 이관 TODO — 진실은 집 PC다

이 리포는 회사 노트북에 있던 번들(v0.38.2)과 스킬로 시드한 상태다.
**최신 작업은 전부 집 PC에서 번들 직수정으로 이뤄졌다** (주말 작업 포함).
cowork의 `core/*` 소스는 그 이전 상태라, 이관 순서는 **집 PC 먼저**다.

**1. 집 PC에서** (`E:\0.리수봇\simcore\` — 여기가 최신):
- [ ] `simcore.plugin.js` 작업 파일이 리포의 v0.38.2와 같은지 확인, 더 최신이면 덮어쓰기
- [ ] `테스트/test-*.js` 커밋 (이거 없이는 아무 데서도 규칙대로 못 고친다)
- [ ] `베리디아/estate-vars.js` 커밋
- [ ] `배포/*.html` 커밋
- [ ] 커밋 후 clone으로 갈아타기 — 기존 E:\ 트리는 참조용으로만

**2. cowork 세션에서** (리포를 pull 받은 뒤):
- [ ] `core/*` 소스, `build.js`를 `legacy-core/` 같은 자리에 일단 커밋 (히스토리 보존)
- [ ] `node build.js` 산출물 vs 리포 번들 diff — **주말 직수정분이 소스에 얼마나 빠졌는지 측정.**
      일치 기대가 아니라 격차 측정이다. 방향은 번들 → 소스 (번들이 진실)
- [ ] 격차를 보고 결정: ① 소스에 전부 포팅해서 빌드 체제 복구 ② 격차가 크면 **번들 단일 파일
      체제로 확정**하고 core/*는 히스토리로만 남김 (테스트가 번들을 직접 물고 있으면 ②도 정당하다)

**3. 아무 쪽이나 마지막에**:
- [ ] 스킬 `SKILL.md`의 `E:\0.리수봇\...` 경로와 "소스는 cowork에만" 문구를 리포 기준으로 갱신
- [ ] `docs/design-주사위-선택지.md` 검토 → checks/choices 구현 (테스트 이관 완료가 선행 조건)

## 작업 환경별 워크플로

- **집 PC** (`E:\0.리수봇\simcore\`): 이 리포를 clone해서 갈아탈 것. 기존 E:\ 트리는 이관 완료 후 참조용으로만
- **회사 노트북**: `C:\claude\simcore` clone
- **cowork / claude.ai**: GitHub 커넥터로 이 리포를 열면 소스·스킬 자동 로드
